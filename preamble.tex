\usepackage{booktabs} % For professional looking tables
\usepackage{array}    % For more control over table columns
\usepackage{longtable} % For tables that span multiple pages
\usepackage{geometry} % To adjust page margins
\usepackage{caption}  % For better control of captions
\usepackage{xcolor}   % For colored text


\geometry{
  left=1in,
  right=1in,
  top=1in,
  bottom=1in
}

% Set custom table styles
\captionsetup{labelfont=bf, font=small, skip=0pt}
\setlength{\abovecaptionskip}{10pt plus 2pt minus 2pt}
\setlength{\belowcaptionskip}{10pt plus 2pt minus 2pt}

\renewcommand{\arraystretch}{1.2} % Increase table row spacing
\setlength{\tabcolsep}{8pt} % Adjust column separation in tables


% Define a color for table rows
\definecolor{tableShade}{gray}{0.9}
\newcommand{\shadeRow}{\rowcolor{tableShade}}
